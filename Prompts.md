"If Express already handles HTTP requests perfectly fine, why did we have to use http.createServer(app) to create a native Node server?"
"What exactly is this CORS error? Why did it pop up when I tried connecting the frontend to the backend, and how did the cors() middleware fix it?"
"Why did we write const io = new Server(server, { cors: {...} }) separately when we already applied CORS to our Express app?"
"Whenever a new user connects, they get a weird socket.id (like XyZ123...). Who generates this ID and why is it needed?"
"When does socket.on('disconnect') actually run? If a user's WiFi suddenly drops, will the server know?"
"Why did we create a /health endpoint in the backend? What does it have to do with a live chat application?"
"Why did we set autoConnect: false during the socket setup on the frontend? What would happen if we left it as true?"
"I don't fully understand this useEffect. Why did we put socket.connect() after the if-condition (if (!isJoined) return;)?"
"For storing messages, why did we use prevChat in setChat((prevChat) => [...prevChat, payload])? Why couldn't we just pass a brand new array directly?"
"What is import.meta.env.VITE_BACKEND_URL in React? Don't people usually use process.env?"
"Is it mandatory to write the cleanup function return () => { socket.off(...) } at the end of the useEffect? Will the app break if I skip it?"
"How does the left and right alignment of the chat bubbles work? How is that isMe variable changing the CSS classes?"
"When I type, how does the server know? And how does the '...is typing' text show up on other people's screens?"
"Why did we put a setTimeout of 1.5 seconds for the typing indicator? Can't the user just press a 'stop_typing' button?"
"What is 'Debouncing' in software engineering? And why was it absolutely necessary to write clearTimeout(typingTimeoutRef.current) in the code?"
"Why did we use useRef to store the typing timer ID? Couldn't I have just used a standard useState for that?"
"When multiple people are typing at the exact same time, how are their names added to the setTypingUsers array without creating duplicates?"
"There was a bug where the screen wouldn't scroll when new messages arrived, and they just hid at the bottom. Why did that happen?"
"What does messagesEndRef = useRef(null) do? And why did we attach it to an empty <div> right at the very end of the chat map?"
"In the new useEffect, we wrote messagesEndRef.current?.scrollIntoView({ behavior: "smooth" }). What is that question mark (?.) doing there, and how does this line work?"
"If 10 or 100 people joined this app right now, would everyone see everyone else's messages? How do we make the chats private?"
"What is the concept of 'Rooms' in Socket.io? How does channel segregation actually work behind the scenes?"
"What does the socket.join(room) function do on the backend? Does it permanently lock the user into that specific room?"
"Earlier we used io.emit(), but in Phase 3 we used io.to(room).emit() and socket.to(room).emit(). What is the actual difference between these three?"
"In the live server's package.json, why did we set the Start Command to node index.js? Why doesn't nodemon work in production?"
